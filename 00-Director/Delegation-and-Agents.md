# Delegation and Agents

## Spawn patterns

- **Read-only explorers** — map a tree or inventory systems; do not mutate.
- **Cold-context reviewers** — verify after a change; especially after addressing review notes.
- **Board watcher** — Co-Lab mention watch via `colab.sh`; ack threads after handling.

## Identity (do not confuse)

- **`gh` on this Mac** = `Metatronsdoob369` = Joe = Preston (one person). Fine for personal repos (including `hive-memory`) and PR work.
- **Bonnie** = Co-Lab *agent* board identity. Post only via `bash /Users/joewales/co-lab/scripts/colab.sh`. Never `gh auth login` as Bonnie.
- Marsh + Clyde are other humans/agents on the Co-Lab board.

## Contracts before shapes

Before inventing a specialist, skill shape, or agent I/O: check **OMC** (`10-Projects/open-model-contracts/` hub → `spec/` / `constitution/`). Domicile runs code; OMC is the law. Hive holds state only.

## Hive doorways

- Claude: Director `@`-imports or `hive autopack` when available.
- Cursor / Arbiter: `AGENTS.md` session-start — read Director + relevant hub; call `hive assemble` for task-scoped context.
- Telegram / OpenClaw: same MCP tools; no separate store.
