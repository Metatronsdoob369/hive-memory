# Graph-Architecture Skill — CURRENT STATE

**What:** crystallized graph-agent *design judgment* as a loadable skill — justification gate, blueprint discipline, house patterns, anti-patterns. Understanding, not harness, not codebase (spectral = a tool it points at).
**Status:** **Specified** (2026-08-09) — spec drafted, awaiting Joe review. Not Tested, not Validated.

## Where we are

- Spec: [SKILL_SPEC.md](SKILL_SPEC.md) — scope, triggers, judgment inventory, eval scenarios, distillation plan.
- Law home eventual = **OMC** (`spec/contracts/v3/skill.ts` conformance — unverified, Mac-side check required). This Hive folder is review parking only.
- Pipeline: NotebookLM compiler persona = replaceable distiller; evidence-labeled Knowledge Map = IR; skill compiles to Claude Code + Co-Lab + Domicile Skills.
- Key call: persona's 18-item blueprint checklist becomes the **eval rubric**, not always-on prose. Skill earns *Tested* only by beating a no-skill baseline on 3 eval scenarios.
- **No memory-architecture reimagining** — skill is additive; restructuring only if its usage produces retrieval failures (backlog note, not a plan).

## Open (Joe)

- Review spec; pick first compile target (Claude Code vs Co-Lab); name it.
