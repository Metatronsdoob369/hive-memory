# Redirects — old cubbies → Hive

Agents hitting old contacts should land here. Prefer auto-follow (thin pointer / stub) over inventing a new store.

| Old contact | → Hive |
| --- | --- |
| `~/.claude/projects/-Users-joewales-Whiteglove/memory/` | `10-Projects/` hubs + `00-Director/`; do not add new memory there |
| ECC `~/.claude/sessions` / homunculus | Coding instincts only; hall memory = this vault |
| `open-model-contracts/src/memory-vault` (CIF) | Hall memory → this vault. Contracts/law stay in OMC repo; hub: `10-Projects/open-model-contracts/` |
| Telekenesis / `domicile_live` agentic-memory | Dormant; use Hive |
| Tripwire `NODE_OUT_Master/bloat/core_intelligence/session_management/CLAUDE_SESSION_STARTUP.js` | Load/print from `00-Director/` (no Firebase required) |
| Per-project stray `MEMORY.md` | Replace with pointer → `10-Projects/<that>/` |
| Arbiter / bond research notes | Hubs link ledger + intelligence-vault HANDOFF; do not cubby copies |
